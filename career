#!/usr/bin/env bash
set -euo pipefail

SELF_PATH="$(python3 -c 'import os,sys; print(os.path.realpath(sys.argv[1]))' "$0")"
SCRIPT_DIR="$(cd "$(dirname "$SELF_PATH")" && pwd)"
exec python3 "$SCRIPT_DIR/watch/watch.py" "$@"
